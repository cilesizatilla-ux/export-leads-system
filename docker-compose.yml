version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: leads_postgres
    environment:
      POSTGRES_USER: leadsuser
      POSTGRES_PASSWORD: leadspass
      POSTGRES_DB: leadsdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leadsuser -d leadsdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: leads_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
